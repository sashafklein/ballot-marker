/* eslint no-undef:0 */

import { spy } from 'sinon';

import { ElectionDetails } from '../';

describe('<ElectionDetails />', () => {
  it('has a test', () => {
    expect(true).to.eq(false);
  });
});
